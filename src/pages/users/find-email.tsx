import BaseLayout from "@/layouts/baseLayout";
import { ReactElement } from "react";

const FindEmailPage = () => {
    return <></>;
};

FindEmailPage.getLayout = (page: ReactElement) => {
    return <BaseLayout>{page}</BaseLayout>;
};

export default FindEmailPage;
