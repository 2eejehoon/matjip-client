import BaseLayout from "@/layouts/baseLayout";
import { ReactElement } from "react";

const FindIdPage = () => {
    return <></>;
};

FindIdPage.getLayout = (page: ReactElement) => {
    return <BaseLayout>{page}</BaseLayout>;
};

export default FindIdPage;
